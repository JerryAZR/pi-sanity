{
  "name": "pi-sanity",
  "version": "0.1.0",
  "description": "Pi extension for sanity checks on agent operations",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node run-tests.js",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "unbash": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
